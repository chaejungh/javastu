package com.kosmo.ex;

public class S03VariableConstant {
    /*

# 📘 2일차 자바 수업 정리: `변수`, `상수`, `데이터 타입`, `이름 규칙`

---

## ✅ 1. 변수(variable / var )란?
int a = 3;
1. 3=> 메모리에 저장 (주소 @f123)
2. 변수 a 생성
3. 변수 a가 3이 저장된 주소(@f123)를 저장

* 데이터를 참조(저장)하는 **이름**
* 변수 선언 시에는 **데이터의 타입**을 명시해야 함

  ```java
  double r = 3; ==> 실제론 3.0 저장됨 (실수 double 이기 때문에)
  ```
* 같은 블럭 `{}` 안에서는 **같은 이름의 변수를 두 번 선언할 수 없다**
* 변수는 한 번 선언한 후에는 **값 변경(참조값 변경)** 가능

```java
r = r / 2; // 변수 r은 이제 1.5를 참조
```

---

## ✅ 2. 상수 (constant), 정보 데이터를 참조하는데 사용함

* 한 번 값이 정해지면 **변경할 수 없는 값**
* `final` 키워드로 선언
* **대문자 스네이크 표기법** 사용 권장 (`PI`, `MAX_VALUE`  등)

```java
final double PI = 3.14;
// PI = 1.14; → 컴파일 오류!
```

---

## ✅ 3. 이름 작성 규칙 (Naming Convention)

| 규칙       | 설명                                  | 예시                           |
| -------- | ----------------------------------- | ---------------------------- |
| **파스칼**  | 클래스명, 첫 글자 대문자, 낱말 구분 시 대문자 사용      | `OpticalMouse`, `HelloWorld` |
| **카멜**   | 변수명, 메서드명, 첫 글자 소문자, 낱말 구분 시 대문자 사용 | `opticalMouse`, `printLine`  |
| **스네이크** | 단어를 `_`로 구분, 상수는 대문자로 표기            | `OPTICAL_MOUSE`, `max_value` |

---

## ✅ 4. 데이터 초기화와 null

### 🔹 기본형(Primitive Type)
*기본형은 타입이 모두 소문자

| 타입      | 초기값 예시  | null 사용 가능 여부 |
| ------- | ------- | ------------- |
| int     | `0`     | ❌             |
| double  | `0.0`   | ❌             |
| boolean | `false` | ❌             |
| char    | `'\0'`  | ❌             |
Boolean c = new Boolean(true); => 자료형
boolean d = false; => 기본형

초기화를 하지 않으면 컴파일 오류 발생
초기화  : 변수 생성과 동시(초기)에 데이터를 참조하는 것.
초기갑이 없는 상태 => undefined
```java
int n;
System.out.println(n); // 오류
```

---

### 🔹 자료/참조형(Reference Type)
자료형 : 복수의 데이터를 관리하기 위해 만들어진 데이터 타입
참조형 : 자료형을 구성하기 위해서 복수의 데이터를 참조하고 있다는 뜻
| 타입     | null 사용 가능 여부 |
| ------ | ------------- |
| 배열, 객체 | ✅             |

초기값:초기화할때 대입하는값

```java
String str = null;
str = null; // 명시적으로 비어있음을 표현
System.out.println(str); // null 출력
```

---

### 🔹 final 변수 초기화

* `final` 변수는 **초기값을 반드시 1회만 설정** 가능
* 초기값을 나중에 대입할 수 있으나, **한 번만 대입 가능**

```java
final int N;
N = 10;   // ✅
N = 20;   // ❌ 오류
```


---

## ✅ 자바 변수/상수/초기화 문제 (2일차 수업 기반)

---

### 📘 문제 1. 다음 설명에 해당하는 용어를 쓰세요.

(1) 데이터를 참조하는 이름으로, 데이터 타입을 명시해야 하며 변경이 가능한 것:
→ `___변수__`

(2) 한 번 값이 정해지면 변경할 수 없고, 보통 대문자로 이름을 작성하는 것:
→ `상수_` constant const, final

---

### 📘 문제 2. 다음 중 올바른 변수 선언은?

A. `int 1num = 3;` error
B. `String s = "Hello";`
C. `final double pi = 3.14;` 상수인데 대문자가 없어서 오답
D. `boolean = false;`
Ans: B, c
---

### 📘 문제 3. 다음 중 자바에서 **변수 이름 규칙**에 어긋나는 것은?

A. `Mouse`
B. `opticalMouse`
C. `optical_mouse`
D. `3value`
Ans: D
---

### 📘 문제 4. 아래 코드 실행 시 출력되는 값을 빈칸에 쓰세요.

```java
double r = 3;
r = r / 2;
System.out.println(r * 2);
```

→ 출력 결과: `3.0`

---

### 📘 문제 5. 다음 코드 중 컴파일 오류가 발생하는 라인은?

```java
final int N;
N = 10;
N = 20;
```
Ans: 마지막 라인
---

### 📘 문제 6. 다음 중 기본형(Primitive Type)이 아닌 것은?

A. `int`
B. `char`
C. `String`
D. `boolean`
Ans: C
---

### 📘 문제 7. 다음 코드 중 오류가 나는 이유를 간단히 설명하세요.

```java
int n;
System.out.println(n);
```

→ 이유: `변수 n에 할당된 값이 없어서 출력이 불가능하다`

---

### 📘 문제 8. 다음 중 참조형 데이터의 **초기값**으로 올바른 것은? B,D

A. `String str = "";`
B. `String str = null;`
C. `String str;` (초기화 없이 사용)
D. `int[] arr = null;`
Ans:B,D
---

### 📘 문제 9. 상수 이름으로 알맞은 것은?

A. `piValue`
B. `PI_VALUE`
C. `PiValue`
D. `Pivalue`
Ans: B
---

### 📘 문제 10. 다음 중 올바른 파스칼 표기법으로 작성된 클래스 이름은?

A. `opticalMouse`
B. `Opticalmouse`
C. `OpticalMouse`
D. `optical_mouse`
Ans:C
---

좋습니다. 문제의 수준과 구성, 실습 연계성을 좀 더 높이도록 개선하겠습니다.
다음은 \*\*2일차 수업(변수, 상수, 자료형, 초기화, 2진수/16진수, 부호 비트)\*\*를 바탕으로 **현장 수업용** 또는 **복습 테스트용**으로 적합한 문제 10문항입니다.

구성 방식은 다음 기준을 반영했습니다:

* **개념 이해 → 코드 해석 → 예측 → 오류 파악** 흐름
* **변수/상수/타입/표기법**과 **이진수/비트/저장 구조** 통합
* 난이도: 초급\~중상급까지 혼합

---

## ✅ 2일차 수업 복습 문제 (개선된 10문항)

---

### 1. 다음 중 자바에서 **정수형 기본형 타입이 아닌 것**은?

A. `byte`
B. `short`
C. `Integer`
D. `long`
Ans:C
---

### 2. 다음 코드에 대한 설명 중 옳은 것을 고르세요.

```java
final int MAX;
MAX = 100;
MAX = 200;
```

A. `MAX = 100;`은 정상 실행된다.
B. 상수는 값을 나중에 한 번은 대입할 수 있으므로 오류가 없다.
C. 두 번째 대입문에서 컴파일 오류가 발생한다.
D. `MAX`는 상수가 아니므로 값 변경이 가능하다.
Ans:A, C
---

### 3. 자바에서 다음 중 **컴파일 오류**가 발생하는 코드는?

A. `byte b = 127;`
B. `int n = 0x10;`
C. `long l = 3000000000;`
D. `char c = '가';`
Ans: C
---

### 4. 2진수 `1001 1010`를 부호 있는 1byte 정수로 해석하면 몇인가?
0110 0110= 6+32+64
(최상위 비트는 부호 비트로 해석)

-26(gpt의 의도)
→ `-102`

---

### 5. 다음 중 16진수로 표현된 값 중 10진수로 `13`과 같은 것은?

A. `0x0D`
B. `0x13`
C. `0x10`
D. `0x0C`
Ans: A
---

### 6. 다음 중 자바 변수 명명 규칙에 **어긋나는 것**은?

A. `opticalMouse`
B. `MAX_VALUE`
C. `1stValue`
D. `_temp` (애매함)
Ans:C
---

### 7. 다음 코드의 출력 결과를 예측하세요.

```java
double r = 3;
r = r / 2;
System.out.println(r * 2);
```

→ 출력 결과: `3.0`

---

### 8. `byte` 타입은 1바이트 크기이며, 표현 가능한 **최소값**은?

A. `-128`
B. `-127`
C. `0`
D. `-256`
Ans: A
---

### 9. 다음 중 4바이트 크기의 정수형 변수 선언은?

A. `byte n = 10;`
B. `short n = 10;`
C. `int n = 10;`
D. `long n = 10;`
Ans: C
---

### 10. 다음 중 **자바에서 숫자 리터럴을 long 타입**으로 인식하도록 하는 방법은?

A. 숫자 뒤에 `L` 또는 `l`을 붙인다.
B. `long`으로 선언만 하면 자동 인식된다.
C. 10자리 이상이면 long으로 인식된다.
D. `int`를 먼저 선언한 뒤 변환한다.
Ans: A
---

    * */

}
